[project]
name = "kitsuyui_ml.animal"
dynamic = ["version"]
description = "A playground for publishing packages to PyPI"
requires-python = ">=3.10"
readme = { file = "README.md", content-type = "text/markdown" }
authors = [
    {name = "Yui KITSU", email = "kitsuyui+github@kitsuyui.com"}
]
classifiers = [
    "Private :: Do Not Upload",
]

[project.urls]
Homepage = "https://github.com/kitsuyui/pypi-playground"

[build-system]
requires = ["setuptools", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[dependency-groups]
dev = [
    "dev-shared",
]

[tool.setuptools]
package-dir = { "" = "src" }
package-data = { "*" = ["README.md, LICENSE", "py.typed", "*.pyi"] }

[tool.setuptools.packages.find]
where = ["src/"]
include = ["kitsuyui_ml.animal"]

[tool.setuptools_scm]
version_file = "./src/kitsuyui_ml/animal/_version.py"
root = "../.."

[tool.uv.sources]
dev-shared = { workspace = true }

[tool.poe]
include = "../dev-shared/poe.tasks.toml"
